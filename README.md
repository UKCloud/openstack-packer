# Using Packer to build Docker-beta version of CoreOS

## Building

To run, simply run the following command:

```
$ packer build ./packer/template.json
```

## Further reading.

There is an online tutorial which contains more information: @TODO